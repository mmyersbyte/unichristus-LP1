// Fa ̧ca um programa usando a linguagem JavaScript que verifique o tipo de dado de uma vari ́avel.
let one = 1;
let um = 'one';
let verdadeiro = true;

function verificaTipo(variavel) {
  console.log(typeof variavel);
}

verificaTipo(one);
verificaTipo(verdadeiro);
verificaTipo(um);
